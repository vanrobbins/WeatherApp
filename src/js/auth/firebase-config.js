/**
 * Your Firebase project's web config.
 *
 * These values are NOT secrets. A Firebase web API key only identifies the
 * project to Google's servers — it grants no access on its own. Access is
 * controlled by Firebase Auth and your Firestore security rules, so this object
 * is safe to ship in client code and commit to the repo.
 *
 * Where to find these: Firebase console → Project settings (gear icon) →
 * "Your apps" → the web app → "SDK setup and configuration" → Config.
 */

export const firebaseConfig = {
  apiKey: 'AIzaSyAt-m81z_NEIkWkv8XLy8a0JCOFUJcpIlk',
  authDomain: 'n423-van.firebaseapp.com',
  projectId: 'n423-van',
  storageBucket: 'n423-van.firebasestorage.app',
  messagingSenderId: '875862911173',
  appId: '1:875862911173:web:fd35face0f873e7537e8ac',
  measurementId: 'G-RTHBBG8003',
};
